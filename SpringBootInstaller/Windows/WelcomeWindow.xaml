<Window x:Class="SpringBootInstaller.Windows.WelcomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MyApp 설치 프로그램"
        Width="800" Height="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBg}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 메인 콘텐츠 -->
        <Border Grid.Row="0" Background="{StaticResource CardBg}" Margin="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel Margin="30,25">
                    <!-- 로고/제목 -->
                <TextBlock Text="ACS 환경 셋팅 설치"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="{StaticResource HeaderText}"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,30"/>

                <!-- 설명 -->
                <TextBlock Text="설치 항목:"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource BodyText}"
                           Margin="0,0,0,15"/>

                <StackPanel Margin="20,0,0,0">
                    <TextBlock Text="• OpenJDK 17 (C:\java)"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,5"/>
                    <TextBlock Text="• Microsoft SQL Server Developer Edition"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,5"/>
                    <TextBlock Text="• 데이터베이스 초기화"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,5"/>
                    <TextBlock Text="• Spring Boot Launcher"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,5"/>
                </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 하단 버튼 -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="취소"
                        Click="CancelButton_Click"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0"/>
                <Button Content="다음 ➜"
                        Click="NextButton_Click"
                        Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
