<Window x:Class="SpringBootInstaller.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설치 설정"
        Width="600" Height="550"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBg}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 메인 콘텐츠 -->
        <Border Grid.Row="0" Background="{StaticResource CardBg}" Margin="20">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="40,30">
                    <TextBlock Text="설치 설정"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource HeaderText}"
                               Margin="0,0,0,30"/>

                    <!-- MSSQL 설정 -->
                    <TextBlock Text="MSSQL 관리자 계정"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,20"/>

                    <Border Background="#EFF6FF"
                            BorderBrush="#3B82F6"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="15"
                            Margin="0,0,0,20">
                        <TextBlock TextWrapping="Wrap"
                                   FontSize="13"
                                   Foreground="#1E40AF">
                            <Run Text="ℹ️ "/>
                            <Run Text="SQL Server SA 계정 정보를 입력하세요."/>
                        </TextBlock>
                    </Border>

                    <!-- SA 아이디 -->
                    <TextBlock Text="SA 계정 아이디:"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,15">
                        <TextBox x:Name="UserIdTextBox"
                                 Text="sa"
                                 Padding="12,10"
                                 FontSize="14"
                                 BorderThickness="0"
                                 TextChanged="UserIdTextBox_Changed"/>
                    </Border>

                    <!-- 비밀번호 -->
                    <TextBlock Text="SA 비밀번호:"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,15">
                        <PasswordBox x:Name="PasswordBox"
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     PasswordChanged="PasswordBox_Changed"/>
                    </Border>

                    <!-- 비밀번호 확인 -->
                    <TextBlock Text="비밀번호 확인:"
                               FontSize="14"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,8"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,30">
                        <PasswordBox x:Name="PasswordConfirmBox"
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     PasswordChanged="PasswordBox_Changed"/>
                    </Border>

                    <!-- 설치 경로 -->
                    <TextBlock Text="애플리케이션 설치 경로"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Background="White">
                            <TextBox x:Name="InstallPathTextBox"
                                     Text="C:\Program Files\MyApp"
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     VerticalContentAlignment="Center"/>
                        </Border>

                        <Button Grid.Column="1"
                                Content="📁 찾아보기"
                                Click="BrowseButton_Click"
                                Style="{StaticResource PrimaryButton}"
                                Margin="10,0,0,0"/>
                    </Grid>

                    <!-- DB 스크립트 폴더 (선택사항) -->
                    <TextBlock Text="DB 스크립트 폴더 (선택사항)"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,20,0,15"/>

                    <Border Background="#EFF6FF"
                            BorderBrush="#3B82F6"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="15"
                            Margin="0,0,0,10">
                        <TextBlock TextWrapping="Wrap"
                                   FontSize="13"
                                   Foreground="#1E40AF">
                            <Run Text="ℹ️ "/>
                            <Run Text="DB 초기화 스크립트가 있는 폴더를 선택하세요. 없으면 비워두면 됩니다."/>
                        </TextBlock>
                    </Border>

                    <Grid Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Background="White">
                            <TextBox x:Name="ScriptsPathTextBox"
                                     Text=""
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     VerticalContentAlignment="Center"
                                     IsReadOnly="True"/>
                        </Border>

                        <Button Grid.Column="1"
                                Content="📁 찾아보기"
                                Click="BrowseScriptsButton_Click"
                                Style="{StaticResource PrimaryButton}"
                                Margin="10,0,0,0"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- 하단 버튼 -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="← 이전"
                        Click="BackButton_Click"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0"/>
                <Button x:Name="NextButton"
                        Content="다음 ➜"
                        Click="NextButton_Click"
                        Style="{StaticResource PrimaryButton}"
                        IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
