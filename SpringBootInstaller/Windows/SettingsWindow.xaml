<Window x:Class="SpringBootInstaller.Windows.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설치 설정"
        Width="800" Height="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PrimaryBg}"
        Icon="/icon/detail_orenge_logo.ico">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 메인 콘텐츠 -->
        <Border Grid.Row="0" Background="{StaticResource CardBg}" Margin="15">
            <StackPanel Margin="20,12">
                    <TextBlock Text="설치 설정"
                               FontSize="22"
                               FontWeight="Bold"
                               Foreground="{StaticResource HeaderText}"
                               Margin="0,0,0,25"/>

                    <!-- 애플리케이션 DB 계정 설정 -->
                    <TextBlock Text="애플리케이션 DB 계정"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,10"/>

                    <Border Background="#EFF6FF"
                            BorderBrush="#3B82F6"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="8"
                            Margin="0,0,0,20">
                        <TextBlock TextWrapping="Wrap"
                                   FontSize="12"
                                   Foreground="#1E40AF">
                            <Run Text="ℹ️ "/>
                            <Run Text="애플리케이션에서 사용할 데이터베이스 계정을 생성합니다."/>
                        </TextBlock>
                    </Border>

                    <!-- 계정 아이디 -->
                    <TextBlock Text="사용자 아이디:"
                               FontSize="13"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,5"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,20">
                        <TextBox x:Name="UserIdTextBox"
                                 Text="acsuser"
                                 Padding="12,10"
                                 FontSize="14"
                                 BorderThickness="0"
                                 TextChanged="UserIdTextBox_Changed"/>
                    </Border>

                    <!-- 비밀번호 -->
                    <TextBlock Text="비밀번호:"
                               FontSize="13"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,5"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,20">
                        <PasswordBox x:Name="PasswordBox"
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     PasswordChanged="PasswordBox_Changed"/>
                    </Border>

                    <!-- 비밀번호 확인 -->
                    <TextBlock Text="비밀번호 확인:"
                               FontSize="13"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,5"/>
                    <Border BorderBrush="{StaticResource BorderColor}"
                            BorderThickness="1"
                            CornerRadius="6"
                            Background="White"
                            Margin="0,0,0,20">
                        <PasswordBox x:Name="PasswordConfirmBox"
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     PasswordChanged="PasswordBox_Changed"/>
                    </Border>

                    <!-- 드라이런 모드 (테스트용) -->
                    <Border Background="#FEF3C7"
                            BorderBrush="#F59E0B"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="10"
                            Margin="0,10,0,20">
                        <StackPanel>
                            <CheckBox x:Name="DryRunCheckBox"
                                      Content="테스트 모드 (드라이런)"
                                      FontSize="14"
                                      FontWeight="SemiBold"
                                      Foreground="#92400E"
                                      VerticalAlignment="Center"/>
                            <TextBlock TextWrapping="Wrap"
                                       FontSize="12"
                                       Foreground="#92400E"
                                       Margin="25,5,0,0">
                                실제 설치 없이 UI와 로직 흐름만 테스트합니다. 시스템을 변경하지 않습니다.
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- DB 스크립트 폴더 (선택사항) -->
                    <TextBlock Text="DB 스크립트 폴더 (선택사항)"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource BodyText}"
                               Margin="0,0,0,8"/>

                    <Border Background="#EFF6FF"
                            BorderBrush="#3B82F6"
                            BorderThickness="1"
                            CornerRadius="6"
                            Padding="8"
                            Margin="0,0,0,20">
                        <TextBlock TextWrapping="Wrap"
                                   FontSize="12"
                                   Foreground="#1E40AF">
                            <Run Text="ℹ️ "/>
                            <Run Text="DB 초기화 스크립트가 있는 폴더를 선택하세요. 없으면 비워두면 됩니다."/>
                        </TextBlock>
                    </Border>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0"
                                BorderBrush="{StaticResource BorderColor}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Background="White">
                            <TextBox x:Name="ScriptsPathTextBox"
                                     Text=""
                                     Padding="12,10"
                                     FontSize="14"
                                     BorderThickness="0"
                                     VerticalContentAlignment="Center"
                                     IsReadOnly="True"/>
                        </Border>

                        <Button Grid.Column="1"
                                Content="📁 찾아보기"
                                Click="BrowseScriptsButton_Click"
                                Style="{StaticResource PrimaryButton}"
                                Margin="10,0,0,0"/>
                    </Grid>
                </StackPanel>
        </Border>

        <!-- 하단 버튼 -->
        <Border Grid.Row="1"
                Background="White"
                BorderBrush="{StaticResource BorderColor}"
                BorderThickness="0,1,0,0"
                Padding="20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="← 이전"
                        Click="BackButton_Click"
                        Style="{StaticResource SecondaryButton}"
                        Margin="0,0,10,0"/>
                <Button x:Name="NextButton"
                        Content="다음 ➜"
                        Click="NextButton_Click"
                        Style="{StaticResource PrimaryButton}"
                        IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
